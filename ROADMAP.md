# מפת דרכים – SLH FULL SUITE

1. הרצה מקומית של `TON-engine` + בדיקת `/site`, `/health`, `/analysis`.
2. הרצה מקומית של `slh-wallet` על פורט (למשל 8081) ובדיקת `/balances`.
3. התאמת `WALLET_BASE_URL` בקובץ `TON-engine/web/app.js` ל-URL האמיתי של שירות הארנק.
4. הרצת `botshop` ולהפוך אותו לשער כניסה/בוט שיווקי.
5. אינטגרציה משולשת:
   - Botshop ↔ TON-engine (ניתוחים אוטומטיים).
   - Botshop ↔ slh-wallet (יתרות/זכויות משתמש).
   - TON-engine ↔ slh-wallet (ניתוח תיקי קהילה).
6. הוספת ML, Dashboard מאוחד, ודוחות אוטומטיים.
